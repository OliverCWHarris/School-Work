#input word, make it into a list, interate throug the list finding the codes for each letter, return the codes into 1 big code and print it


def str_to_lst(input_str):
    str_lst=input_str.list()
    return str_lst

def char_to_code(str_lst):
    e